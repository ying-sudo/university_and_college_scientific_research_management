export function getDeleteMap() {
    var delete_map = new Map([
        ["loginlog", "mangerSys/loginlog"], 
        ["systemlog", "mangerSys/syslog"], 
        ["operationlog", "mangerSys/operlog"], 
       
    ]);

    return delete_map;
}