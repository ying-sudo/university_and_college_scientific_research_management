<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.sicnu.dao.FundDao">

    <resultMap type="cn.edu.sicnu.entity.Fund" id="FundMap">
        <result property="id" column="id" jdbcType="VARCHAR"/>
        <result property="projectId" column="project_id" jdbcType="VARCHAR"/>
        <result property="requestFund" column="request_fund" jdbcType="NUMERIC"/>
        <result property="arrivalFund" column="arrival_fund" jdbcType="NUMERIC"/>
        <result property="requestDate" column="request_date" jdbcType="TIMESTAMP"/>
        <result property="arrivalDate" column="arrival_date" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="FundMap">
        select id, project_id, request_fund, arrival_fund, request_date, arrival_date
        from scientific_research.fund
        where id = #{id}
    </select>
    <select id="getFundByProjectId" resultMap="FundMap">
        select id, project_id, request_fund, arrival_fund, request_date, arrival_date
        from scientific_research.fund
        where project_id = #{id}
    </select>
    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="FundMap">
        select id, project_id, request_fund, arrival_fund, request_date, arrival_date
        from scientific_research.fund
                     limit #{offset}, #{limit}
    </select>
    <!--查询表所有对象-->
    <select id="findAll" resultMap="FundMap">
        select id, project_id, request_fund, arrival_fund, request_date, arrival_date
        from scientific_research.fund
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="FundMap">
        select
        id, project_id, request_fund, arrival_fund, request_date, arrival_date
        from scientific_research.fund
        <where>
            <if test="id != null and id != ''">
                and id = #{id}
            </if>
            <if test="projectId != null and projectId != ''">
                and project_id = #{projectId}
            </if>
            <if test="requestFund != null">
                and request_fund = #{requestFund}
            </if>
            <if test="arrivalFund != null">
                and arrival_fund = #{arrivalFund}
            </if>
            <if test="requestDate != null">
                and request_date = #{requestDate}
            </if>
            <if test="arrivalDate != null">
                and arrival_date = #{arrivalDate}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.fund (project_id, request_fund, arrival_fund, request_date, arrival_date)
        values (#{projectId}, #{requestFund}, #{arrivalFund}, #{requestDate}, #{arrivalDate})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.fund(project_id, request_fund, arrival_fund, request_date, arrival_date)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.projectId}, #{entity.requestFund}, #{entity.arrivalFund}, #{entity.requestDate},
            #{entity.arrivalDate})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.fund(project_id, request_fund, arrival_fund, request_date, arrival_date)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.projectId}, #{entity.requestFund}, #{entity.arrivalFund}, #{entity.requestDate},
            #{entity.arrivalDate})
        </foreach>
        on duplicate key update
        project_id = values(project_id) , request_fund = values(request_fund) , arrival_fund = values(arrival_fund) ,
        request_date = values(request_date) , arrival_date = values(arrival_date)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update scientific_research.fund
        <set>
            <if test="projectId != null and projectId != ''">
                project_id = #{projectId},
            </if>
            <if test="requestFund != null">
                request_fund = #{requestFund},
            </if>
            <if test="arrivalFund != null">
                arrival_fund = #{arrivalFund},
            </if>
            <if test="requestDate != null">
                request_date = #{requestDate},
            </if>
            <if test="arrivalDate != null">
                arrival_date = #{arrivalDate},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from scientific_research.fund
        where id = #{id}
    </delete>

</mapper>