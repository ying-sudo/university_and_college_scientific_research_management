<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.sicnu.dao.SortDao">

    <resultMap type="cn.edu.sicnu.entity.Sort" id="SortMap">
        <result property="id" column="id" jdbcType="VARCHAR"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="value" column="value" jdbcType="VARCHAR"/>
        <result property="note" column="note" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="SortMap">
        select id, name, value, note
        from scientific_research.sort
        where id = #{id}
    </select>
    <select id="selectByNote" resultMap="SortMap">
        select id, name, value, note
        from scientific_research.sort
        where note = #{note}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="SortMap">
        select id, name, value, note
        from scientific_research.sort
                     limit #{offset}, #{limit}
    </select>
    <!--查询表所有对象-->
    <select id="findAll" resultMap="SortMap">
        select id, name, value, note
        from scientific_research.sort
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="SortMap">
        select
        id, name, value, note
        from scientific_research.sort
        <where>
            <if test="id != null and id != ''">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="value != null and value != ''">
                and value = #{value}
            </if>
            <if test="note != null and note != ''">
                and note = #{note}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.sort (name, value, note)
        values (#{name}, #{value}, #{note})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.sort(name, value, note)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.value}, #{entity.note})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.sort(name, value, note)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.value}, #{entity.note})
        </foreach>
        on duplicate key update
        name = values(name) , value = values(value) , note = values(note)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update scientific_research.sort
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="value != null and value != ''">
                value = #{value},
            </if>
            <if test="note != null and note != ''">
                note = #{note},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from scientific_research.sort
        where id = #{id}
    </delete>

</mapper>