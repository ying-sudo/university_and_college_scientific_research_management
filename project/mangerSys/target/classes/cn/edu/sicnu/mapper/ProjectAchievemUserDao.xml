<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.sicnu.dao.ProjectAchievemUserDao">

    <resultMap type="cn.edu.sicnu.entity.ProjectAchievemUser" id="ProjectAchievemUserMap">
        <result property="id" column="id" jdbcType="VARCHAR"/>
        <result property="userId" column="user_id" jdbcType="VARCHAR"/>
        <result property="contribution" column="contribution" jdbcType="INTEGER"/>
        <result property="category" column="category" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="ProjectAchievemUserMap">
        select id, user_id, contribution, category
        from scientific_research.project_achievem_user
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="ProjectAchievemUserMap">
        select id, user_id, contribution, category
        from scientific_research.project_achievem_user
                     limit #{offset}, #{limit}
    </select>
    <!--查询表所有对象-->
    <select id="findAll" resultMap="ProjectAchievemUserMap">
        select id, user_id, contribution, category
        from scientific_research.project_achievem_user
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="ProjectAchievemUserMap">
        select
        id, user_id, contribution, category
        from scientific_research.project_achievem_user
        <where>
            <if test="id != null and id != ''">
                and id = #{id}
            </if>
            <if test="userId != null and userId != ''">
                and user_id = #{userId}
            </if>
            <if test="contribution != null">
                and contribution = #{contribution}
            </if>
            <if test="category != null">
                and category = #{category}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.project_achievem_user (id,user_id, contribution, category)
        values (#{id},#{userId}, #{contribution}, #{category})
    </insert>

    <insert id="insertBatch" >
        insert into scientific_research.project_achievem_user(user_id, contribution, category)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}, #{entity.contribution}, #{entity.category})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.project_achievem_user(user_id, contribution, category)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}, #{entity.contribution}, #{entity.category})
        </foreach>
        on duplicate key update
        user_id = values(user_id) , contribution = values(contribution) , category = values(category)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update scientific_research.project_achievem_user
        <set>
            <if test="userId != null and userId != ''">
                user_id = #{userId},
            </if>
            <if test="contribution != null">
                contribution = #{contribution},
            </if>
            <if test="category != null">
                category = #{category},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from scientific_research.project_achievem_user
        where id = #{id}
    </delete>

</mapper>