<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.sicnu.dao.CollegeDao">

    <resultMap type="cn.edu.sicnu.entity.College" id="CollegeMap">
        <result property="id" column="id" jdbcType="VARCHAR"/>
        <result property="location" column="location" jdbcType="VARCHAR"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="snumber" column="snumber" jdbcType="INTEGER"/>
        <result property="information" column="information" jdbcType="VARCHAR"/>
        <result property="state" column="state" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="CollegeMap">
        select id, location, name, snumber, information, state
        from scientific_research.college
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="CollegeMap">
        select id, location, name, snumber, information, state
        from scientific_research.college
                     limit #{offset}, #{limit}
    </select>
    <!--查询表所有对象-->
    <select id="findAll" resultMap="CollegeMap">
        select id, location, name, snumber, information, state
        from scientific_research.college
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="CollegeMap">
        select
        id, location, name, snumber, information, state
        from scientific_research.college
        <where>
            <if test="id != null and id != ''">
                and id = #{id}
            </if>
            <if test="location != null and location != ''">
                and location = #{location}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="snumber != null">
                and snumber = #{snumber}
            </if>
            <if test="information != null and information != ''">
                and information = #{information}
            </if>
            <if test="state != null">
                and state = #{state}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.college (location, name, information, state)
        values (#{location}, #{name}, #{information}, #{state})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.college(location, name, information, state)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.location}, #{entity.name}, #{entity.information}, #{entity.state})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.college(location, name, information, state)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.location}, #{entity.name}, #{entity.information}, #{entity.state})
        </foreach>
        on duplicate key update
        location = values(location) , name = values(name) , information = values(information) , state = values(state)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update scientific_research.college
        <set>
            <if test="location != null and location != ''">
                location = #{location},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="information != null and information != ''">
                information = #{information},
            </if>
            <if test="state != null">
                state = #{state},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from scientific_research.college
        where id = #{id}
    </delete>

</mapper>