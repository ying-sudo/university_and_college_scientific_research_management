<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.sicnu.dao.MagazineDao">

    <resultMap type="cn.edu.sicnu.entity.Magazine" id="MagazineMap">
        <result property="id" column="id" jdbcType="VARCHAR"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="isShow" column="is_show" jdbcType="OTHER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="MagazineMap">
        select id, name, is_show
        from scientific_research.magazine
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="MagazineMap">
        select id, name, is_show
        from scientific_research.magazine
                     limit #{offset}, #{limit}
    </select>
    <!--查询表所有对象-->
    <select id="findAll" resultMap="MagazineMap">
        select id, name, is_show
        from scientific_research.magazine
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="MagazineMap">
        select
        id, name, is_show
        from scientific_research.magazine
        <where>
            <if test="id != null and id != ''">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="isShow != null">
                and is_show = #{isShow}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.magazine (name, is_show)
        values (#{name}, #{isShow})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.magazine(name, is_show)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.isShow})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.magazine(name, is_show)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.isShow})
        </foreach>
        on duplicate key update
        name = values(name) , is_show = values(is_show)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update scientific_research.magazine
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="isShow != null">
                is_show = #{isShow},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from scientific_research.magazine
        where id = #{id}
    </delete>

</mapper>