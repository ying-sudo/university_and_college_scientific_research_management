<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.sicnu.dao.BatchDao">

    <resultMap type="cn.edu.sicnu.entity.Batch" id="BatchMap">
        <result property="id" column="id" jdbcType="VARCHAR"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="beginDate" column="begin_date" jdbcType="TIMESTAMP"/>
        <result property="endData" column="end_data" jdbcType="TIMESTAMP"/>
        <result property="state" column="state" jdbcType="INTEGER"/>
        <result property="collegeId" column="college_id" jdbcType="VARCHAR"/>
        <result property="path" column="path" jdbcType="VARCHAR"/>
        <result property="note" column="note" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="BatchMap">
        select id,
               name,
               begin_date,
               end_data,
               state,
               college_id,
               path,
               note
        from scientific_research.batch
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="BatchMap">
        select id,
               name,
               begin_date,
               end_data,
               state,
               college_id,
               path,
               note
        from scientific_research.batch
                     limit #{offset}, #{limit}
    </select>
    <!--查询表所有对象-->
    <select id="findAll" resultMap="BatchMap">
        select id,
               name,
               begin_date,
               end_data,
               state,
               college_id,
               path,
               note
        from scientific_research.batch
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="BatchMap">
        select
        id, name, begin_date, end_data, state, college_id, path, note
        from scientific_research.batch
        <where>
            <if test="id != null and id != ''">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="beginDate != null">
                and begin_date = #{beginDate}
            </if>
            <if test="endData != null">
                and end_data = #{endData}
            </if>
            <if test="state != null">
                and state = #{state}
            </if>
            <if test="collegeId != null and collegeId != ''">
                and college_id = #{collegeId}
            </if>
            <if test="path != null and path != ''">
                and path = #{path}
            </if>
            <if test="note != null and note != ''">
                and note = #{note}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.batch (name, begin_date, state, college_id, path, note)
        values (#{name}, #{beginDate}, #{state}, #{collegeId}, #{path}, #{note})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.batch(name, begin_date, state, college_id, path, note)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.beginDate}, #{entity.state}, #{entity.collegeId}, #{entity.path}, #{entity.note})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.batch(name, begin_date, state, college_id, path, note)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.beginDate}, #{entity.state}, #{entity.collegeId}, #{entity.path}, #{entity.note})
        </foreach>
        on duplicate key update
        name = values(name) , begin_date = values(begin_date) , state = values(state) , college_id = values(college_id)
        , path = values(path) , note = values(note)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update scientific_research.batch
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="beginDate != null">
                begin_date = #{beginDate},
            </if>
            <if test="state != null">
                state = #{state},
            </if>
            <if test="collegeId != null and collegeId != ''">
                college_id = #{collegeId},
            </if>
            <if test="path != null and path != ''">
                path = #{path},
            </if>
            <if test="note != null and note != ''">
                note = #{note},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from scientific_research.batch
        where id = #{id}
    </delete>

</mapper>