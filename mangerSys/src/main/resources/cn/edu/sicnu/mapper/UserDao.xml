<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.sicnu.dao.UserDao">

    <resultMap type="cn.edu.sicnu.entity.Users" id="UserMap">
        <result property="id" column="id" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="phone" column="phone" jdbcType="VARCHAR"/>
        <result property="email" column="email" jdbcType="VARCHAR"/>
        <result property="idNumber" column="id_number" jdbcType="VARCHAR"/>
        <result property="collegeId" column="college_id" jdbcType="VARCHAR"/>
        <result property="register" column="register" jdbcType="TIMESTAMP"/>
        <result property="note" column="note" jdbcType="VARCHAR"/>
        <result property="sex" column="sex" jdbcType="VARCHAR"/>
        <result property="birthDate" column="birth_date" jdbcType="TIMESTAMP"/>
        <result property="post" column="post" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserMap">
        select id,
               password,
               name,
               phone,
               email,
               id_number,
               college_id,
               register,
               note,
               sex,
               birth_date,
               post
        from scientific_research.users
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserMap">
        select id,
               password,
               name,
               phone,
               email,
               id_number,
               college_id,
               register,
               note,
               sex,
               birth_date,
               post
        from scientific_research.users
                     limit #{offset}, #{limit}
    </select>
    <!--查询表所有对象-->
    <select id="findAll" resultMap="UserMap">
        select id,
               password,
               name,
               phone,
               email,
               id_number,
               college_id,
               register,
               note,
               sex,
               birth_date,
               post
        from scientific_research.users
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="UserMap">
        select
        id, password, name, phone, email, id_number, college_id, register, note, sex, birth_date, post
        from scientific_research.users
        <where>
            <if test="id != null and id != ''">
                and id = #{id}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="phone != null and phone != ''">
                and phone = #{phone}
            </if>
            <if test="email != null and email != ''">
                and email = #{email}
            </if>
            <if test="idNumber != null and idNumber != ''">
                and id_number = #{idNumber}
            </if>
            <if test="collegeId != null and collegeId != ''">
                and college_id = #{collegeId}
            </if>
            <if test="register != null">
                and register = #{register}
            </if>
            <if test="note != null and note != ''">
                and note = #{note}
            </if>
            <if test="sex != null and sex != ''">
                and sex = #{sex}
            </if>
            <if test="birthDate != null">
                and birth_date = #{birthDate}
            </if>
            <if test="post != null and post != ''">
                and post = #{post}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.users (password, name, phone, id_number, college_id, register, note)
        values (#{password}, #{name}, #{phone}, #{idNumber}, #{collegeId}, #{register}, #{note})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.users(password, name, phone, id_number, college_id, register, note)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.password}, #{entity.name}, #{entity.phone}, #{entity.idNumber}, #{entity.collegeId},
            #{entity.register}, #{entity.note})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.users(password, name, phone, id_number, college_id, register, note)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.password}, #{entity.name}, #{entity.phone}, #{entity.idNumber}, #{entity.collegeId},
            #{entity.register}, #{entity.note})
        </foreach>
        on duplicate key update
        password = values(password) , name = values(name) , phone = values(phone) , id_number = values(id_number) ,
        college_id = values(college_id) , register = values(register) , note = values(note)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update scientific_research.users
        <set>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
            <if test="idNumber != null and idNumber != ''">
                id_number = #{idNumber},
            </if>
            <if test="collegeId != null and collegeId != ''">
                college_id = #{collegeId},
            </if>
            <if test="register != null">
                register = #{register},
            </if>
            <if test="note != null and note != ''">
                note = #{note},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from scientific_research.users
        where id = #{id}
    </delete>
    <!--登录-->
    <select id="findByIdAndPassword" resultMap="UserMap">
        select id,
               password,
               name,
               phone,
               email,
               id_number,
               college_id,
               register,
               note,
               sex,
               birth_date,
               post
        from scientific_research.users where id=#{id} and password=#{password}
    </select>
</mapper>