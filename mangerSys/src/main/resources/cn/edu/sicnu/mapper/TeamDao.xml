<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.sicnu.dao.TeamDao">

    <resultMap type="cn.edu.sicnu.entity.Team" id="TeamMap">
        <result property="id" column="id" jdbcType="VARCHAR"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="userId" column="user_id" jdbcType="VARCHAR"/>
        <result property="researchDiection" column="research_diection" jdbcType="VARCHAR"/>
        <result property="discipline" column="discipline" jdbcType="VARCHAR"/>
        <result property="firstDiscipline" column="first_discipline" jdbcType="VARCHAR"/>
        <result property="foundingTime" column="founding_time" jdbcType="TIMESTAMP"/>
        <result property="phone" column="phone" jdbcType="VARCHAR"/>
        <result property="information" column="information" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="TeamMap">
        select id,
               name,
               user_id,
               research_diection,
               discipline,
               first_discipline,
               founding_time,
               phone,
               information
        from scientific_research.team
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="TeamMap">
        select id,
               name,
               user_id,
               research_diection,
               discipline,
               first_discipline,
               founding_time,
               phone,
               information
        from scientific_research.team
                     limit #{offset}, #{limit}
    </select>
    <!--查询表所有对象-->
    <select id="findAll" resultMap="TeamMap">
        select id,
               name,
               user_id,
               research_diection,
               discipline,
               first_discipline,
               founding_time,
               phone,
               information
        from scientific_research.team
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="TeamMap">
        select
        id, name, user_id, research_diection, discipline, first_discipline, founding_time, phone, information
        from scientific_research.team
        <where>
            <if test="id != null and id != ''">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="userId != null and userId != ''">
                and user_id = #{userId}
            </if>
            <if test="researchDiection != null and researchDiection != ''">
                and research_diection = #{researchDiection}
            </if>
            <if test="discipline != null and discipline != ''">
                and discipline = #{discipline}
            </if>
            <if test="firstDiscipline != null and firstDiscipline != ''">
                and first_discipline = #{firstDiscipline}
            </if>
            <if test="foundingTime != null">
                and founding_time = #{foundingTime}
            </if>
            <if test="phone != null and phone != ''">
                and phone = #{phone}
            </if>
            <if test="information != null and information != ''">
                and information = #{information}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.team (id,name,
                                              user_id,
                                              research_diection,
                                              discipline,
                                              first_discipline,
                                              founding_time,
                                              information)
        values (#{id},#{name},
                #{userId},
                #{researchDiection},
                #{discipline},
                #{firstDiscipline},
                #{foundingTime},
                #{information})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.team(name, user_id, research_diection, discipline, first_discipline,
        founding_time, information)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.userId}, #{entity.researchDiection}, #{entity.discipline},
            #{entity.firstDiscipline}, #{entity.foundingTime}, #{entity.information})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.team(name, user_id, research_diection, discipline, first_discipline,
        founding_time, information)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.userId}, #{entity.researchDiection}, #{entity.discipline},
            #{entity.firstDiscipline}, #{entity.foundingTime}, #{entity.information})
        </foreach>
        on duplicate key update
        name = values(name) , user_id = values(user_id) , research_diection = values(research_diection) , discipline =
        values(discipline) , first_discipline = values(first_discipline) , founding_time = values(founding_time) ,
        information = values(information)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update scientific_research.team
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="userId != null and userId != ''">
                user_id = #{userId},
            </if>
            <if test="researchDiection != null and researchDiection != ''">
                research_diection = #{researchDiection},
            </if>
            <if test="discipline != null and discipline != ''">
                discipline = #{discipline},
            </if>
            <if test="firstDiscipline != null and firstDiscipline != ''">
                first_discipline = #{firstDiscipline},
            </if>
            <if test="foundingTime != null">
                founding_time = #{foundingTime},
            </if>
            <if test="information != null and information != ''">
                information = #{information},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from scientific_research.team
        where id = #{id}
    </delete>

</mapper>