<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.sicnu.dao.RightsDao">

    <resultMap type="cn.edu.sicnu.entity.Rights" id="RightsMap">
        <result property="id" column="id" jdbcType="VARCHAR"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="sort" column="sort" jdbcType="INTEGER"/>
        <result property="rightIndex" column="right_index" jdbcType="INTEGER"/>
        <result property="isFather" column="is_father" jdbcType="OTHER"/>
        <result property="urlAddress" column="url_address" jdbcType="VARCHAR"/>
        <result property="note" column="note" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="RightsMap">
        select id, name, sort, right_index, is_father, url_address, note
        from scientific_research.rights
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="RightsMap">
        select id, name, sort, right_index, is_father, url_address, note
        from scientific_research.rights
                     limit #{offset}, #{limit}
    </select>
    <!--查询表所有对象-->
    <select id="findAll" resultMap="RightsMap">
        select id, name, sort, right_index, is_father, url_address, note
        from scientific_research.rights
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="RightsMap">
        select
        id, name, sort, right_index, is_father, url_address, note
        from scientific_research.rights
        <where>
            <if test="id != null and id != ''">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="sort != null">
                and sort = #{sort}
            </if>
            <if test="rightIndex != null">
                and right_index = #{rightIndex}
            </if>
            <if test="isFather != null">
                and is_father = #{isFather}
            </if>
            <if test="urlAddress != null and urlAddress != ''">
                and url_address = #{urlAddress}
            </if>
            <if test="note != null and note != ''">
                and note = #{note}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.rights (name, sort, right_index, is_father, url_address, note)
        values (#{name}, #{sort}, #{rightIndex}, #{isFather}, #{urlAddress}, #{note})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.rights(name, sort, right_index, is_father, url_address, note)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.sort}, #{entity.rightIndex}, #{entity.isFather}, #{entity.urlAddress},
            #{entity.note})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into scientific_research.rights(name, sort, right_index, is_father, url_address, note)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.sort}, #{entity.rightIndex}, #{entity.isFather}, #{entity.urlAddress},
            #{entity.note})
        </foreach>
        on duplicate key update
        name = values(name) , sort = values(sort) , right_index = values(right_index) , is_father = values(is_father) ,
        url_address = values(url_address) , note = values(note)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update scientific_research.rights
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="sort != null">
                sort = #{sort},
            </if>
            <if test="rightIndex != null">
                right_index = #{rightIndex},
            </if>
            <if test="isFather != null">
                is_father = #{isFather},
            </if>
            <if test="urlAddress != null and urlAddress != ''">
                url_address = #{urlAddress},
            </if>
            <if test="note != null and note != ''">
                note = #{note},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from scientific_research.rights
        where id = #{id}
    </delete>

</mapper>